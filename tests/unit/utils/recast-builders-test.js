import recastBuilders from 'ast-maker/utils/recast-builders';
import { module, test } from 'qunit';

module('Unit | Utility | recast-builders', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = recastBuilders();
    assert.ok(result);
  });
});
