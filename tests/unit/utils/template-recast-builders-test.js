import templateRecastBuilders from 'ast-builder/utils/template-recast-builders';
import { module, test } from 'qunit';

module('Unit | Utility | template-recast-builders', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = templateRecastBuilders();
    assert.ok(result);
  });
});
